using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SwipeIdent : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
       /* String[] Direction = new String[4];
        Direction[1] = "Up";
        Direction[2] = "Down";
        Direction[3] = "Left";
        Direction[4] = "Right";
        SwipeData obj;
        if (obj.Direction == Up)
        {

        } */
    }
}
